# Copiar a la raíz del repositorio como `.env.axonbim` (ignorado por git vía `.env.*`).
# `scripts/dev/run_dev.sh` hace `source` de ese archivo antes de `linux_profile.sh`.
#
# AXONBIM_GPU_PROFILE=auto
# AXONBIM_GPU_PROFILE=integrated
# AXONBIM_GPU_PROFILE=dedicated
